# Donor App

Backend system for managing donor programs in college organizations.

## Tech Stack
- Java 21
- Spring Boot
- Maven

## Phase 1
Single-layer architecture with in-memory CRUD operations for User.

Endpoints:
POST   /users
GET    /users
GET    /users/{id}
PUT    /users/{id}
DELETE /users/{id}